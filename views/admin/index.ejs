<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">

  <header class="bg-white border-b sticky top-0 z-10">
    <div class="max-w-3xl mx-auto px-4 py-3 flex items-center justify-between gap-4">
      <div>
        <h1 class="text-sm font-semibold text-gray-800">Product Admin</h1>
        <p class="text-xs text-gray-400 mt-0.5">Internal use only</p>
      </div>
      <div class="flex items-center gap-2 shrink-0">
        <a href="/admin/new"
           class="text-xs bg-blue-500 text-white px-3 py-1.5 rounded-lg hover:bg-blue-600 transition-colors">
          + New Product
        </a>
        <form method="POST" action="/logout">
          <button type="submit"
                  class="text-xs border border-gray-300 text-gray-600 px-3 py-1.5 rounded-lg hover:bg-gray-50 transition-colors">
            Logout
          </button>
        </form>
      </div>
    </div>
  </header>

  <main class="max-w-3xl mx-auto px-4 py-6 space-y-6">

    <section class="bg-white rounded-2xl shadow-sm p-5">
      <h2 class="text-sm font-semibold text-gray-700 mb-3">NFC Label URL</h2>
      <div class="flex gap-2 items-center">
        <input id="domain-input" type="text" placeholder="https://your-domain"
               class="flex-1 border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-300">
        <span class="text-xs text-gray-400">The URL below updates automatically</span>
      </div>
    </section>

    <% if (products.length === 0) { %>
    <div class="text-center py-16 text-gray-400 text-sm">
      No product data yet. Click "+ New Product" to start.
    </div>
    <% } else { %>
    <div class="space-y-4">
      <% products.forEach(function(p) { %>
      <% const displayName = p.nameEn || 'English name pending'; %>
      <div class="bg-white rounded-2xl shadow-sm p-5">
        <div class="flex items-start justify-between gap-4 mb-4">
          <div>
            <span class="text-base font-bold text-gray-900"><%= p.model %></span>
            <span class="text-sm text-gray-500 ml-2"><%= displayName %></span>
          </div>
          <div class="flex gap-2 shrink-0">
            <a href="/admin/<%= p.model %>/edit"
               class="text-xs border border-blue-400 text-blue-500 px-3 py-1 rounded-lg hover:bg-blue-50 transition-colors">
              Edit
            </a>
            <form method="POST" action="/admin/products/<%= p.model %>/delete"
                  data-model="<%= p.model %>"
                  data-name="<%= displayName %>"
                  onsubmit="return confirmDelete(this)">
              <button type="submit"
                      class="text-xs border border-red-300 text-red-400 px-3 py-1 rounded-lg hover:bg-red-50 transition-colors">
                Delete
              </button>
            </form>
          </div>
        </div>

        <div class="space-y-2">
          <div class="flex items-center gap-2">
            <span class="text-xs text-gray-400 w-16 shrink-0">URL (ZH)</span>
            <code class="flex-1 text-xs bg-gray-50 border rounded px-2 py-1 text-gray-700 truncate url-display"
                  data-path="/<%= p.model %>"></code>
            <button onclick="copyUrl(this)"
                    class="copy-button text-xs bg-gray-100 hover:bg-gray-200 text-gray-600 px-2 py-1 rounded transition-colors shrink-0">
              Copy
            </button>
          </div>
          <div class="flex items-center gap-2">
            <span class="text-xs text-gray-400 w-16 shrink-0">URL (EN)</span>
            <code class="flex-1 text-xs bg-gray-50 border rounded px-2 py-1 text-gray-700 truncate url-display"
                  data-path="/<%= p.model %>?lang=en"></code>
            <button onclick="copyUrl(this)"
                    class="copy-button text-xs bg-gray-100 hover:bg-gray-200 text-gray-600 px-2 py-1 rounded transition-colors shrink-0">
              Copy
            </button>
          </div>
        </div>
      </div>
      <% }); %>
    </div>
    <% } %>

  </main>

  <script>
    const domainInput = document.getElementById('domain-input');

    function updateUrls() {
      const domain = (domainInput.value || '').replace(/\/$/, '');
      document.querySelectorAll('.url-display').forEach((el) => {
        el.textContent = domain + el.getAttribute('data-path');
      });
    }

    domainInput.addEventListener('input', updateUrls);
    updateUrls();

    function copyUrl(btn) {
      const code = btn.previousElementSibling;
      const text = code.textContent;
      if (!text || text.startsWith('/')) {
        alert('Enter a domain first');
        return;
      }
      navigator.clipboard.writeText(text).then(() => {
        btn.dataset.copied = '1';
        btn.textContent = 'Copied ✓';
        btn.classList.add('bg-green-100', 'text-green-600');
        setTimeout(() => {
          btn.textContent = 'Copy';
          btn.classList.remove('bg-green-100', 'text-green-600');
          delete btn.dataset.copied;
        }, 1500);
      });
    }

    function confirmDelete(form) {
      return confirm(`Delete ${form.dataset.model || ''} ${form.dataset.name || ''}? This action cannot be undone.`);
    }
  </script>
</body>
</html>
